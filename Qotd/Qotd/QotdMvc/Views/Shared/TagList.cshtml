@model Qotd.Entities.TagEntry[]
@foreach (var t in Model)
{
    if (!String.IsNullOrEmpty(t.Value))
    {
        <div class="tag">
            @if (t.Approved)
            {
            <a href="@Url.Action("Tag", "Home", new { tagId = t.TagId })">#@t.Value</a>
                }
                else
                {
                <text>#@t.Value</text>
            }
        </div> 
    }
}
